{% extends 'base.html.twig' %}

{% block title %}CompetitionMatch index{% endblock %}

{% block body %}
    <h1>CompetitionMatch index</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Home Team</th>
                <th>Away Team</th>
                <th>Start_date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>

{#        {% for team in teams %}#}
{#            <tr>#}
{#                <td>{{ team }}</td>#}
{#                <td>{{ loop.index }}</td>  loop.index is the current iteration index #}
{#                <td>{{ loop.index0 }}</td>  loop.index0 is the current iteration index starting from 0 #}
{#            </tr>#}
{#        {% endfor %}#}


        {% for i in 0..teams|length-2 %}
            {% for j in i+1..teams|length-1%}
                {%
                    set team1 = teams[i]
                %}

                {%
                    set team2 = teams[j]
                %}
                {%
                    set day = i + 4 * j
                %}
                {%
                set month = day//30 + 1
                %}
            <tr>
                <td>{{ team1 }}</td>
                <td>{{ team2 }}</td>
                <td>{{ day }}</td>
                <td>{{ month }}</td>
            </tr>

        {% endfor %}
        {% endfor %}

        {% for competition_match in competition_matches %}
            <tr>

                <td>{{ competition_match.id }}</td>
                <td>{{ competition_match.getName() }}</td>
                <td>{{ competition_match.getName() }}</td>
                <td>{{ competition_match.startDate ? competition_match.startDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_competition_match_show', {'id': competition_match.id}) }}">show</a>
                    <a href="{{ path('app_competition_match_edit', {'id': competition_match.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_competition_match_new') }}">Create new</a>
    <br>


{#    <a href="{{ path('app_competition_match_index', {'buttonId': 'schedule-matches'}) }}" class="btn btn-primary">Generate a schedule for your matches</a>#}
{#    <br>#}


    <button type="button" class="btn btn-primary click">Click me!</button>

<script>document.querySelector('.click').addEventListener('click', (e) => {
        // Do whatever you want
        e.target.textContent = 'Clicked!';
    });
</script>


{% endblock %}
