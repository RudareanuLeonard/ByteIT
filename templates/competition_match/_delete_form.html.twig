<form method="post" action="{{ path('app_competition_match_delete', {'id': competition_match.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competition_match.id) }}">
    <button class="btn">Delete</button>
</form>
